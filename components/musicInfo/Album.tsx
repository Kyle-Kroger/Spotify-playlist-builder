import styled from "styled-components";

const Wrapper = styled.div`
  display: flex;
`;

const Album = (props) => {
  const { name, artist, showArtistName, imageUrl } = props;
};

export default Album;
